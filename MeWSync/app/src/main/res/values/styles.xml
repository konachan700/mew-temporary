<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorBrandDark3</item>
        <item name="colorAccent">@color/colorBrandDefaultElement</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="RoundButtonTheme" parent="Theme.AppCompat.Light">
        <item name="android:textColor">#555555</item>
        <item name="android:minHeight">77dp</item>
        <item name="android:minWidth">77dp</item>
        <item name="android:layout_margin">1dp</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/x00_round_button_01_bg</item>
    </style>
</resources>
